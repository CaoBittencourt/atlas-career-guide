#' @export
box::use(
  mod / utils / bin / bin[...],
  mod / utils / bin / grid[...],
  mod / utils / bin / kde[...],
  mod / utils / bin / kmeans[...],
  mod / utils / bin / pdf[...],
  mod / utils / bin / sample[...],
  mod / utils / bin / stats[...],
)
